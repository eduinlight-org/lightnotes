[package]
name = "landing"
version = "0.1.0"
edition = "2021"

[dependencies]
dioxus = { workspace = true, features = ["router"] }
ui = { workspace = true }
dioxus-icons = { version = "0.1.0", default-features = false }

[features]
default = []
web = ["dioxus/web"]
server = ["dioxus/fullstack", "dioxus/server", "ui/server"]
